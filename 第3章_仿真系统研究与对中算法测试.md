# 第3章 仿真系统研究

## 3.1 研究目标
本章针对激光对中问题，基于 `Laser_GUIcs4_17.m` 建立双端光路三维仿真系统。研究目标是构建一套可重复、可调参、可视化的分析平台，用于描述不对中状态下光束传播与PSD接收响应之间的几何关系。

与单向或理想化模型相比，本系统同时考虑M侧与S侧安装误差、内部角度偏差和旋转过程中的姿态耦合，可较完整反映工程装调场景中的几何特征。

## 3.2 仿真系统总体架构
系统由“参数输入—几何建模—光线追迹—结果显示”四个模块组成。

1. 参数输入模块：完成不对中参数、结构参数及两侧安装误差的设置。
2. 几何建模模块：建立M轴、S轴、安装基准点和局部坐标系。
3. 光线追迹模块：计算双向光路与接收面的交点及局部坐标输出。
4. 结果显示模块：输出三维结构图、全局投影图、局部PSD图和角度响应图。

该架构对应代码中的 `run_simulation` 主流程，能够在同一界面内完成参数修改、模型更新与结果联动显示。

## 3.3 参数体系与变量定义

### 3.3.1 不对中参数
系统采用以下不对中参数描述两轴相对状态：

- 水平偏心：\(h\)（mm）
- 垂直偏心：\(k\)（mm）
- 水平倾角输入：\(\alpha_u\)（mm/100mm）
- 垂直倾角输入：\(\beta_u\)（mm/100mm）

角度换算关系为：

\[
\alpha = \arctan\left(\frac{\alpha_u}{100}\right),
\quad
\beta  = \arctan\left(\frac{\beta_u}{100}\right)
\]

### 3.3.2 结构参数与误差参数
结构参数包括轴向距离 `M_len_val` 与安装半径基准 `Ld_base`。误差参数按两侧分别设置，包含：

- 安装高度误差
- 机箱滚转角（roll）
- 机箱俯仰角（pitch）
- 机箱偏航角（yaw）
- 光源内部俯仰角偏差
- 光源内部偏航角偏差

该参数体系满足“几何位置误差 + 姿态误差 + 光源指向误差”的联合建模需求。

## 3.4 三维几何建模方法

### 3.4.1 轴线与参考点建模
M侧轴线起点定义为：

\[
\mathbf{M}_{start} = [0,\ h,\ k]^T
\]

根据不对中倾角构建轴线方向向量：

\[
\mathbf{v}=[1,\ \tan\alpha,\ \tan\beta]^T,
\quad
\mathbf{U}_{axis}=\frac{\mathbf{v}}{\|\mathbf{v}\|}
\]

S侧轴线按程序设定在 \(x=-M\_len\_val\) 处建立参考起点，方向取全局 \(+X\) 方向。由此形成双端统一全局坐标框架。

### 3.4.2 截面基底与旋转描述
为表征旋转角 \(\theta\) 下的空间位置，程序在轴线法截面上建立径向基 \(\mathbf{n}_r\) 与切向基 \(\mathbf{n}_t\)，并在 \(0\sim 2\pi\) 区间等间隔采样。对每个采样点，机箱姿态按“滚转—俯仰—偏航”顺序更新。

姿态更新采用Rodrigues旋转形式：

\[
\mathbf{v}_{rot}=\mathbf{v}\cos\phi + (\mathbf{k}\times\mathbf{v})\sin\phi + \mathbf{k}(\mathbf{k}\cdot\mathbf{v})(1-\cos\phi)
\]

该实现对应 `rodrigues_rot` 与 `build_box_frame`，并在每步后执行单位化与正交化处理，以控制数值误差累积。

## 3.5 光线追迹与PSD坐标映射

### 3.5.1 双向光路建模
系统同时计算两条光路：

1. M-LD \(\rightarrow\) S-PSD
2. S-LD \(\rightarrow\) M-PSD

每条光线通过“参数方程 + 平面方程”求交。若光线起点为 \(\mathbf{P}_0\)、方向为 \(\mathbf{d}\)，接收面上一点为 \(\mathbf{P}_{plane}\)、法向为 \(\mathbf{n}\)，则交点参数为：

\[
t = \frac{\mathbf{n}\cdot(\mathbf{P}_{plane}-\mathbf{P}_0)}{\mathbf{n}\cdot\mathbf{d}}
\]

命中点为：

\[
\mathbf{P}_{hit}=\mathbf{P}_0+t\mathbf{d}
\]

该过程由 `line_plane_intersection` 实现，并对平行退化情形进行有效性判断。

### 3.5.2 PSD局部坐标输出
命中点映射到PSD局部坐标系后得到二维输出：

\[
u = (\mathbf{P}_{hit}-\mathbf{P}_{PSD})\cdot\mathbf{e}_r,
\quad
v = (\mathbf{P}_{hit}-\mathbf{P}_{PSD})\cdot\mathbf{e}_t
\]

其中，\(\mathbf{e}_r\) 与 \(\mathbf{e}_t\) 分别为接收面径向与切向单位向量。该输出直接对应程序中的 `uv_S_*` 与 `uv_M_*` 变量。

## 3.6 可视化实现与系统输出
系统输出包含以下三类结果：

1. 三维结构结果：展示双轴、两侧器件位置及双向光束轨迹；
2. 全局投影结果：在Y-Z平面显示理想与误差工况轨迹差异；
3. 局部响应结果：在PSD局部坐标中显示轨迹形状与零点偏移，并给出 \(X-\theta\) 关系曲线。

通过上述结果可直观观察不同参数对轨迹中心、轨迹形态和周期响应的影响，为后续章节实验比对提供统一的数据接口和图形依据。

## 3.7 本章小结
本章完成了激光对中仿真系统的建模与实现，给出了参数体系、几何关系、光线求交方法及可视化输出机制。基于 `Laser_GUIcs4_17.m` 的系统具备参数可控、过程可复现、结果可解释的特点，能够满足毕业论文中“仿真系统研究”部分的写作与分析需求。

从论文写作规范看，本章结构遵循常见工程类本科论文写法（研究目标—系统架构—数学模型—实现输出—小结），可直接作为第3章主体内容使用。
