# 第3章 仿真系统研究与对中算法测试

## 3.1 研究目标与总体思路
本章围绕双端激光对中问题，基于 `Laser_GUIcs4_17.m` 构建三维几何仿真系统，对“理想工况—安装误差工况”下的光斑轨迹进行对比分析。系统通过双向光路（M侧LD到S侧PSD、S侧LD到M侧PSD）同步计算，得到全局投影轨迹、PSD局部坐标输出及角度响应曲线，为后续实验验证提供可观测指标。

与仅关注单向光路的简化模型不同，该模型显式考虑两侧机箱安装偏差、内部光源偏转以及旋转过程中的姿态耦合，从而更接近工程现场的装调过程。

## 3.2 仿真系统结构设计

### 3.2.1 参数化输入模块
界面输入参数可分为四类：

1. 对中状态参数：水平偏心 `h`、垂直偏心 `k`、水平倾角 `alpha`、垂直倾角 `beta`（以 `mm/100mm` 形式输入后换算为角度）。
2. 几何参数：两端轴向距离 `M_len_val`、安装半径基准 `Ld_base`。
3. M侧安装与内部误差：包括高度偏差、滚转（roll）、俯仰（pitch）、偏航（yaw），以及LD内部俯仰/偏航。
4. S侧安装与内部误差：参数定义与M侧一致。

该输入结构可覆盖“轴系不对中 + 传感器安装误差 + 光源内部误差”的组合工况，具备批量扫参与灵敏度分析基础。

### 3.2.2 多视图输出模块
程序设置7个主要输出坐标轴：

1. 三维总览图：显示M轴、S轴、两条光束命中轨迹和结构示意。
2. S侧全局投影（Y-Z）。
3. M侧全局投影（Y-Z）。
4. S-PSD局部坐标输出（`u_S-v_S`）。
5. M-PSD局部坐标输出（`u_M-v_M`）。
6. S侧 `X-θ` 曲线。
7. M侧 `X-θ` 曲线。

上述输出分别对应“空间结构验证、测面轨迹形态、传感器读数响应、角度耦合特征”四类分析需求。

## 3.3 对中算法建模方法

### 3.3.1 全局轴线与基准构建
M侧轴线起点定义为：

\[
\mathbf{M}_{start}=[0,\ h,\ k]^T
\]

根据输入倾角构建方向向量：

\[
\mathbf{v}=[1,\ \tan\alpha,\ \tan\beta]^T,
\quad \mathbf{U}_{axis}=\frac{\mathbf{v}}{\|\mathbf{v}\|}
\]

程序中将M侧轴段长度取为 `2*M_len_val`，由此得到M轴终点与中心位置。S侧轴线则以 `x=-M_len_val` 的位置建立参考轴，方向取 `+X`。

### 3.3.2 截面基底与旋转参数化
为描述转角 `θ` 下的径向/切向方向，程序在M轴与S轴分别建立截面正交基 `n_r, n_t`，并在 `0~360°` 范围内等间隔采样（360点）。

在每个 `θ` 位置，机箱安装姿态通过三步旋转构建：

1. 绕轴向滚转（roll）；
2. 绕当前切向俯仰（pitch）；
3. 绕当前径向偏航（yaw）。

该过程由 `build_box_frame` 实现，并在每一步后进行单位化与正交化，降低数值累计误差。

### 3.3.3 光源发射方向与误差注入
理想工况下，光束方向沿各自轴线；误差工况下，发射方向通过Rodrigues旋转公式叠加内部俯仰与偏航：

\[
\mathbf{v}_{rot}=\mathbf{v}\cos\theta + (\mathbf{k}\times\mathbf{v})\sin\theta + \mathbf{k}(\mathbf{k}\cdot\mathbf{v})(1-\cos\theta)
\]

对应代码函数为 `rodrigues_rot`。该方法避免欧拉角矩阵在小角度复合时的书写冗长，便于逐项注入误差并追踪误差来源。

### 3.3.4 光线-平面求交与PSD输出映射
两条光路分别计算：

1. M-LD → S-PSD；
2. S-LD → M-PSD。

命中点由 `line_plane_intersection` 求得，其核心为：

\[
t=\frac{\mathbf{n}\cdot(\mathbf{P}_{plane}-\mathbf{P}_0)}{\mathbf{n}\cdot\mathbf{d}},
\quad \mathbf{P}_{hit}=\mathbf{P}_0+t\mathbf{d}
\]

得到命中点后，进一步投影到接收面局部基底，形成PSD二维输出：

\[
u=\mathbf{r}\cdot\mathbf{e}_r,
\quad v=\mathbf{r}\cdot\mathbf{e}_t
\]

其中 `r` 为命中点相对PSD中心的位移向量。

## 3.4 仿真测试流程与评价指标

### 3.4.1 测试流程
本研究采用“先理想、后误差”的对照流程：

1. 误差参数置零，得到理想基准轨迹；
2. 逐项施加安装高度、滚转、俯仰、偏航及LD内部角误差；
3. 比较双向光路在全局坐标和局部PSD坐标下的轨迹变化；
4. 分析 `X-θ` 曲线偏移与波动，判断误差耦合特征。

### 3.4.2 评价指标
建议采用以下指标表征对中性能：

1. 轨迹中心偏移量：衡量系统静态偏心误差；
2. 轨迹长短轴变化：衡量角误差引起的各向异性；
3. 峰峰值（Peak-to-Peak）：衡量旋转过程波动幅度；
4. 双向一致性误差：比较M→S与S→M结果差异，评估系统对称性；
5. 局部坐标零点偏移：直接对应PSD安装与标定修正量。

## 3.5 关键仿真现象分析
依据程序结构与输出图形，可得到如下规律：

1. 偏心参数 `h,k` 主要引起轨迹整体平移；
2. 倾角参数 `alpha,beta` 主要引起轨迹形状变化与角度相关调制；
3. 机箱滚转对局部坐标系方向影响显著，表现为轨迹旋转；
4. 俯仰/偏航与LD内部偏转共同作用时，会产生非对称畸变；
5. 双向光路在误差注入后一致性下降，可用于定位误差主要来源于哪一侧。

上述结论可为现场调试提供顺序建议：先修正静态偏心，再修正姿态角误差，最后处理内部光学安装误差。

## 3.6 本章小结
本章基于 `Laser_GUIcs4_17.m` 构建了双LD-双PSD三维仿真系统，完成了从几何建模、误差注入、光线追迹到多维可视化输出的完整流程。该系统能够在统一平台下对理想与误差工况进行可重复对比，为第4章实验验证提供了明确的测试变量、可观测量与评估指标。

从工程应用角度看，该模型不仅可用于论文验证，也可扩展为装调辅助工具：通过拟合实测PSD轨迹反求安装误差参数，实现“仿真—实测—修正”的闭环对中。
